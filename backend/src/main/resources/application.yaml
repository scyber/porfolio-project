
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/personalization
    username: postgres-user
    password: postgres-pw

topics:
  events: events
  profiles: profiles

logging:
  level:
    root: INFO